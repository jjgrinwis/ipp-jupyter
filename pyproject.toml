[project]
name = "ipp-jupyter"
version = "0.1.0"
description = "Jupyter notebook demo for the Akamai Prolexic IP Protect Configuration API"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "edgegrid-python>=2.0.7",
    "jupyter>=1.1.1",
]
